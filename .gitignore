__pycache__/
*.pyc
*.egg-info/
build/
dist/
.pytest_cache/
.DS_Store
.metadata_never_index
pytest-report.xml
# `python -m conformance` writes records carrying interpreter paths, a worker
# registry directory and environment overrides. They are local evidence, and
# this repository is public.
conformance-results/
